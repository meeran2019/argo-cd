apiVersion: v1
kind: Pod
metadata:
  annotations:
    kubernetes.io/psp: eks.privileged
  labels:
    run: nginx
  name: nginx-22
  namespace: default
spec:
  containers:
  - image: nginx
    name: nginx